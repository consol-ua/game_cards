const initialState = {
  data: [
    {
      name: "Petia",
      data: "17.12.2020",
      timeGame: "00:12:30",
      gameLevel: "level_1"
    }
  ]
};

const result_reducer = (state = initialState, action) => {
  return state;
};

export default result_reducer;
